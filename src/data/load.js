const STATUS = ['NEW', 'POSTED', 'ASSIGNED', 'SHIPPED'];

const STATE = ['En route to Pick Up', 'Arrived to Pick Up', 'En route to delivery', 'Arrived to delivery', null];

module.exports = {
  STATE, STATUS,
};
